@model IEnumerable<Clothes>

@{
    var pageName = ViewBag.CurrentServices;
    ViewData["Title"] = pageName;
}

<h2>@ViewBag.CurrentServices</h2>
<hr />

@foreach (var clothes in Model)
{
    @Html.Partial("ClothesSummary", clothes)
}
<br />
